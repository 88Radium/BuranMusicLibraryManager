<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:BuranUI.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="BuranUI.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Icon="/Assets/avalonia-logo.ico"
        Opacity="0.8"
        Title="BuranUI">
    <!-- x:DataType="vm:MainWindowViewModel" -->
    <!-- xmlns:local="clr-namespace:BuranUI.ViewModels" -->
    
    
    <Design.DataContext>
        <!-- This only sets the DataContext for the previewer in an IDE,
             to set the actual DataContext for runtime, set the DataContext property in code (look at App.axaml.cs) -->
        
        <vm:MainWindowViewModel />
        <!-- <local:MainWindowViewModel x:Uid="VM"/> -->
    </Design.DataContext>


    <Grid>
        <!-- <TextBlock Text="{Binding Greeting}" HorizontalAlignment="Center" VerticalAlignment="Center"/> -->
        <TabControl ItemsSource="{Binding Tabs}" />
        <TextBlock Text="Hallo Stefan und Thomas Wunder!" FontSize="56" />
    </Grid>
</Window>